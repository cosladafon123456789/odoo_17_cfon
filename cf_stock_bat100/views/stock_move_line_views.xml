<odoo>
    <!-- Wizard/árbol de líneas de movimiento: añadir BAT100 junto al número de serie -->
    <record id="view_move_line_tree_bat100" model="ir.ui.view">
        <field name="name">stock.move.line.tree.bat100</field>
        <field name="model">stock.move.line</field>
        <field name="inherit_id" ref="stock.view_move_line_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='lot_id']" position="after">
                <field name="bat100" widget="boolean_toggle"/>
            </xpath>
        </field>
    </record>

    <!-- Form de línea de movimiento (cuando se abre la ventanita individual) -->
    <record id="view_stock_move_line_form_bat100" model="ir.ui.view">
        <field name="name">stock.move.line.form.bat100</field>
        <field name="model">stock.move.line</field>
        <field name="inherit_id" ref="stock.view_stock_move_line_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='lot_id']" position="after">
                <field name="bat100"/>
            </xpath>
        </field>
    </record>
</odoo>