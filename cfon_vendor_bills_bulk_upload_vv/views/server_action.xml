<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Server action bound to account.move to appear in 'Acción' menu -->
    <record id="server_open_vendor_bill_upload_wizard" model="ir.actions.server">
        <field name="name">Subir facturas (PDF)</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="binding_model_id" ref="account.model_account_move"/>
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="code">
action = env.ref('cfon_vendor_bills_bulk_upload.action_account_vendor_bill_upload_wizard').read()[0]
action
        </field>
    </record>
</odoo>
