<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Bind the wizard to the Action menu for account.move (list and form) -->
    <record id="action_account_vendor_bill_upload_wizard_bound" model="ir.actions.act_window">
        <field name="name">Subir facturas (PDF)</field>
        <field name="res_model">account.vendor.bill.upload.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="binding_model_id" ref="account.model_account_move"/>
        <field name="binding_view_types">list,form</field>
    </record>
</odoo>
